import { Customer } from "@/types/factors";
export declare const customerAtom: import("jotai").WritableAtom<Customer[], [typeof import("jotai/utils").RESET | Customer[] | ((prev: Customer[]) => typeof import("jotai/utils").RESET | Customer[])], void>;
